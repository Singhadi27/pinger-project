static_resources:
  listeners:
  - name: pinger_listener
    address:
      socket_address:
        address: 0.0.0.0
        port_value: 10000
    filter_chains:
    - filters:
      - name: envoy.filters.network.http_connection_manager
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
          stat_prefix: pinger_http
          route_config:
            name: local_route
            virtual_hosts:
            - name: pinger_routes
              domains: ["*"]
              routes:
              - match:
                  prefix: "/"
                route:
                  cluster: pinger_app
          http_filters:
          - name: envoy.filters.http.router

  clusters:
  # 🔹 Pinger application (same pod)
  - name: pinger_app
    connect_timeout: 5s
    type: logical_dns
    lb_policy: round_robin
    load_assignment:
      cluster_name: pinger_app
      endpoints:
      - lb_endpoints:
        - endpoint:
            address:
              socket_address:
                address: 127.0.0.1
                port_value: 3000

admin:
  access_log_path: /tmp/admin_access.log
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 9901

